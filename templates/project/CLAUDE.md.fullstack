# {{PROJECT_NAME}} — Claude Code Instructions

## What Is This?
{{PROJECT_DESCRIPTION}}

## Tech Stack
{{TECH_STACK}}

## Architecture
{{ARCHITECTURE_NOTES}}

## Quality Standards
- WCAG AA accessibility
- OWASP Top 10 security
- SEO fundamentals
- Self-hosted fonts
- No hardcoded versions — always check live
- Input validation at system boundaries

## Database
- Migrations: Always create migration files, never edit schema directly
- Seed data: Separate from production data
- Backups: Verify backup strategy exists

## Auth & Security
- No secrets in code — use environment variables
- Session management: Secure, HttpOnly, SameSite cookies
- Password hashing: bcrypt with sufficient rounds
- CSRF protection on all forms

## MCP Servers
<!-- If this project uses MCP servers that return large results (DB schemas, API specs),
     configure the maxResultSizeChars annotation in the MCP server response:
     _meta: { "anthropic/maxResultSizeChars": 500000 }
     This prevents truncation of large results (Claude Code 2.1.91+). -->

## Git & CI/CD
- Conventional Commits (feat:, fix:, docs:, chore:)
- Feature branches, PR reviews
- Tests before commit
- After push: `gh run list --limit 1` — fix on failure

## Error Handling
- On error: stop, explain, suggest alternative
- Root cause analysis: SYMPTOM → CONTEXT → CAUSE → FIX → PREVENTION

## Deployment
{{DEPLOYMENT_NOTES}}
